
// Firebase removed; this file no longer initializes Firebase.

// Firebase removed; exports remain only for compatibility.
export const db = undefined as never;
export const auth = undefined as never;
export default {} as never;
